[project]
name = "sdk-client"
version = "0.1.0"
description = "Standard OpenAI-SDK chat/responses test clients for the prunus dev server"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.44.0",
]
